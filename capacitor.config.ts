import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yasmeen.miniapp',
  appName: 'Mini App',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;