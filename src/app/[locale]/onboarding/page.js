// SSR -> Onboarding + Main Configuration

import Onboarding from '@/components/onboarding';

export default function OnboardingPage() {
  return (
    <div>
      <Onboarding />
    </div>
  );
}